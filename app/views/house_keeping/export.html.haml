%table
  %tr
    %th player
    %th mountpoint
    %th item
    %th cs_type
    %th parent_item_name
  - @players.each do |player|
    - player.items.not_trash.each do |item|
      !=render :partial =>"items", :locals => {:player => player, :item => item}

