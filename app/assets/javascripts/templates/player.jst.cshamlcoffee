= @player.get("name")
- for inventory in @player.get("inventories")
  != JST["templates/inventory"].call(inventory: new Page.Models.Inventory(inventory))

- for bag in @player.get("bags")
  != JST["templates/bag"].call(bag: new Page.Models.Bag(bag))
