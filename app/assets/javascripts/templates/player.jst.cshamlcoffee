#{@player.get("name")} is carrying #{@player.get("weight")}kg of stuff
.container-fluid
  .row-fluid
    .span6
      - for slot in @player.get("slots")
        != JST["templates/slot"].call(slot: new Page.Models.Slot(slot))
    .span6
      - for bag in @player.get("bags")
        != JST["templates/bag"].call(bag: new Page.Models.Bag(bag))
