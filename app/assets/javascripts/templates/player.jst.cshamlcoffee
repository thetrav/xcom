.tab-pane{"id":"player#{@player.id}"}
  #{@player.get("name")} is carrying
  %span.weight{"id":"player-#{@player.id}-weight"}= @player.weight()
  kg of stuff
  .controls{"id":"player-#{@player.id}-controls"}
    %button.btn.addItem add item
  .items{"id":"player#{@player.id}"}
