.slot{"id": @slot.get("id")}
  = @slot.get("name")
  - if @slot.get("item")
    != JST["templates/item"].call(item: new Page.Models.Item(@slot.get("item")))
  - else
    .item-picker{"data-id": @slot.get("id")} No Item
